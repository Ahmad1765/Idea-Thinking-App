import 'package:flutter/material.dart';

abstract class AppColors {
  static const primary = Color(0xFF4B0082);
  static const accent = Color(0xFF00FFFF);
  static const background = Color(0xFF0A0A0F);
  static const surfaceGlass = Color(0x0DFFFFFF); // rgba(255,255,255,0.05)
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFA0A0B0);
  static const glassBorder = Color(0x2600FFFF); // rgba(0,255,255,0.15)
  static const error = Color(0xFFFF4D4D);
  static const success = Color(0xFF00FF88);

  // Swipe overlays
  static const swipeRight = Color(0xFF00FFFF);
  static const swipeLeft = Color(0xFFFF4D4D);
}
